{%if tag == true %} 
<small class="alert-success">
    le role {{ roles  }} a été confisqué à {{username}}!
</small>
{%elseif tag == false %}
<small class="alert-warning">
    {{ username }} n'a pas encore ce role {{ roles  }}
    </small>
{% endif %}{# empty Twig template #}
